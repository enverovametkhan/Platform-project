version: 2.1

orbs:
  path-filtering: circleci/path-filtering@1.0.0

workflows:
  setup-workflow:
    jobs:
      - path-filtering/filter:
          name: Updates
          base-revision: main
          config-path: .circleci/continue-config.yml
          mapping: |
            backend/.* backend-job true
            frontend/.* frontend-job true
      filters: 
        branches:
          only:
            - dev
            - release
            - main
